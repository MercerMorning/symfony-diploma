{% extends 'base.html.twig' %}

{% block body %}

    {% for video in videos %}
        <a href="{{ path('app_editor', {video: video.name}) }}">
            {{ video.name }}
        </a>
    {% endfor %}
{% endblock %}